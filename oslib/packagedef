Описание
	// Имя библиотеки. Используется при именовании файлов пакета
	// и в `opm install <Имя>`
	.Имя("component")
	
	// Имя библиотеки не тождественно имени компоненты. В рамках одной библиотеки может быть несколько компонент.

	// Версия самой библиотеки
	.Версия("1.0.0")

	// `opm build` соберёт файл по шаблону <Имя>-<Версия>.ospx

	// Зависимости библиотеки (opm будет их устанавливать автоматически)
	//.ЗависитОт("ИмяНужнойБиблиотеки")
	//.ЗависитОт("ЕщёОдноИмяНужнойБиблиотеки")
	
	// Зависимости разработки библиотеки (opm будет их устанавливать автоматически при включенном флаге -dev)
	
	//.РазработкаЗависитОт("ЕщёОдноИмяНужнойБиблиотеки")

	// Имя автора
	.Автор("Гениальный автор")
	// и почта для связи
	.АдресАвтора("genius@example.com")

	// Краткое описание, что же даёт эта библиотека людям
	.Описание("Очень нужная библиотека с кучей полезных плюшек")

	// .ВерсияСреды("2.0.0")   // <- Если необходима определённая версия движка

	// По соглашению сюда будут складываться компоненты (см. appveyor.yml)
	.ВключитьФайл("Components")

	// Подробное описание библиотеки для VSCode
	.ВключитьФайл("README.md")
;

// vim: filetype=onescript
